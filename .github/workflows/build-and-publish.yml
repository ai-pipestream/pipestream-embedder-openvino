name: Build and Publish

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-publish:
    uses: ai-pipestream/.github/.github/workflows/reusable-service-build.yml@main
    with:
      docker-image-name: pipestream-embedder-openvino
      dockerfile-path: runtime/src/main/docker/Dockerfile.jvm
    secrets: inherit
